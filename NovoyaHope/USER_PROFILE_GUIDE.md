# Руководство по работе с профилем пользователя

## 🎯 Что было добавлено

Реализована полноценная система профилей пользователей с следующими возможностями:

### 1. **Аватар пользователя в навигационной панели**
   - Красивое отображение аватара пользователя в верхней панели
   - Если фото не загружено, отображается иконка по умолчанию
   - Плавная анимация при наведении

### 2. **Выпадающее меню профиля**
   - Открывается при клике на аватар пользователя
   - Отображает:
     - Большой аватар
     - Имя и фамилию пользователя (или email, если не указаны)
     - Email пользователя (кликабельный - открывает модальное окно)
   - Содержит быстрые ссылки:
     - Мой профиль
     - Мои опросы
     - Настройки
     - Выход

### 2.1 **Модальное окно информации о пользователе**
   - Открывается при клике на email в выпадающем меню
   - Красивая анимация появления
   - Отображает полную информацию:
     - Большой аватар профиля
     - Имя и фамилию
     - Email с возможностью копирования в буфер обмена
     - Телефон с учетом настроек приватности
     - Уникальный ID пользователя
   - Функции:
     - Копирование email/телефона одним кликом
     - Быстрый переход к редактированию профиля
     - Закрытие по клику вне окна или на Escape
   - Индикация успешного копирования (галочка с анимацией)

### 3. **Страница профиля пользователя**
   - Редактирование основной информации:
     - Имя
     - Фамилия
     - Email
     - Телефон
     - Настройка видимости телефона (показывать всем или только себе)
   - Загрузка фото профиля:
     - Поддержка JPG, PNG, GIF
     - Drag & drop интерфейс
     - Превью изображения перед сохранением
     - Автоматическое удаление старого фото при загрузке нового
   - Изменение пароля:
     - Модальное окно для безопасного изменения пароля
     - Валидация текущего пароля
     - Подтверждение нового пароля

### 4. **Дизайн и UX**
   - Современный Material Design стиль
   - Плавные анимации и переходы
   - Адаптивная верстка для мобильных устройств
   - Красивые градиенты и тени
   - Информативные сообщения об успехе/ошибке

## 📦 Установка и настройка

### Шаг 1: Применить миграцию базы данных

Миграция уже создана в файле `Migrations/20251127000000_AddUserProfileFields.cs`

Чтобы применить ее к базе данных, выполните:

```bash
dotnet ef database update
```

Или из Package Manager Console в Visual Studio:

```
Update-Database
```

### Шаг 2: Проверка структуры папок

Убедитесь, что существует папка для загрузки изображений:
- `wwwroot/uploads/profiles/`

Если папки нет, она будет создана автоматически при первой загрузке изображения.

### Шаг 3: Запуск приложения

```bash
dotnet run
```

## 🚀 Использование

### Просмотр информации о пользователе:

1. **Войдите в систему**
2. **Кликните на аватар** в правом верхнем углу
3. **Кликните на email** в выпадающем меню
4. **Откроется модальное окно** с полной информацией:
   - Просмотрите все данные профиля
   - Скопируйте email или телефон одним кликом
   - Перейдите к редактированию профиля

### Редактирование профиля:

1. **Войдите в систему** или зарегистрируйтесь
2. **Кликните на аватар** в правом верхнем углу
3. **Выберите "Мой профиль"**
4. **Заполните информацию:**
   - Укажите имя и фамилию
   - Добавьте номер телефона
   - Настройте видимость телефона
   - Загрузите фото профиля
5. **Нажмите "Сохранить изменения"**

### Для изменения пароля:

1. На странице профиля нажмите **"Изменить пароль"**
2. Введите текущий пароль
3. Введите новый пароль (минимум 6 символов)
4. Подтвердите новый пароль
5. Нажмите **"Изменить пароль"**

## 🎨 Компоненты системы

### Модель данных (ApplicationUser)

Добавлены новые поля:
- `FirstName` - Имя пользователя (до 50 символов)
- `LastName` - Фамилия пользователя (до 50 символов)
- `ProfileImagePath` - Путь к фото профиля (до 500 символов)
- `ShowPhoneToPublic` - Показывать ли телефон всем (bool, по умолчанию false)
- `PhoneNumber` - Телефон (уже был в IdentityUser)

### Контроллер (AccountController)

Новые методы:
- `Profile()` [GET] - Отображение страницы профиля
- `Profile(ProfileViewModel)` [POST] - Сохранение изменений профиля
- `ChangePassword(ChangePasswordViewModel)` [POST] - Изменение пароля

### Представления

- `Views/Account/Profile.cshtml` - Страница профиля пользователя
- `Views/Shared/_Layout.cshtml` - Обновленная навигация с аватаром

### Стили

- `wwwroot/css/layout.css` - Стили для:
  - Аватара и выпадающего меню
  - Модального окна информации о пользователе
  - Адаптивная верстка для всех устройств
- Встроенные стили в `Profile.cshtml` для страницы профиля

## 🔒 Безопасность

- Все действия с профилем требуют авторизации (`[Authorize]`)
- Пароли валидируются ASP.NET Core Identity
- Загружаемые файлы сохраняются с уникальными именами
- Старые фото автоматически удаляются при загрузке новых
- CSRF защита через AntiForgeryToken
- Приватность телефона - пользователь контролирует видимость
- Копирование данных работает только на стороне клиента (безопасно)

## 📱 Адаптивность

Интерфейс полностью адаптирован для:
- Desktop (1024px+)
- Tablet (768px - 1024px)
- Mobile (до 768px)

## 🎯 Возможности для расширения

### Дополнительные поля профиля:
- Дата рождения
- Пол
- Город
- О себе (биография)
- Социальные сети

### Функционал:
- Подтверждение email
- Подтверждение телефона
- Двухфакторная аутентификация
- История входов
- Настройки уведомлений
- Приватность профиля

### Интеграции:
- OAuth провайдеры (Google, Facebook, VK)
- Загрузка аватара из социальных сетей
- Email уведомления об изменениях профиля

## 🐛 Устранение неполадок

### Проблема: Не применяется миграция

**Решение:**
```bash
dotnet ef migrations list
dotnet ef database update
```

### Проблема: Ошибка при загрузке изображения

**Проверьте:**
1. Существует ли папка `wwwroot/uploads/profiles/`
2. Имеет ли приложение права на запись в эту папку
3. Размер файла не превышает лимит (настройте в `appsettings.json`)

### Проблема: Не отображается аватар в навигации

**Проверьте:**
1. Путь к изображению в базе данных (должен начинаться с `/uploads/profiles/`)
2. Существует ли файл физически на диске
3. Нет ли ошибок в консоли браузера (F12)

### Проблема: Не открывается модальное окно при клике на email

**Проверьте:**
1. Загружен ли jQuery и другие скрипты
2. Нет ли JavaScript ошибок в консоли (F12)
3. Пользователь авторизован в системе

### Проблема: Не копируется email/телефон

**Решение:**
- Функция копирования работает только в HTTPS или на localhost
- Убедитесь, что браузер поддерживает Clipboard API
- Проверьте разрешения браузера для буфера обмена

## 📄 Лицензия

Этот компонент является частью проекта NovoyaHope Forms.

---

**Версия:** 1.0  
**Дата:** 27 ноября 2025  
**Автор:** NovoyaHope Development Team

