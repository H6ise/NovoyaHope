# Инструкция по созданию администратора

В системе NovoyaHope Forms есть несколько способов создать администратора. Ниже описаны все доступные методы.

## Способ 1: Автоматическое создание при первом запуске (Рекомендуется)

При первом запуске приложения система автоматически создает администратора по умолчанию.

### Данные для входа:
- **Email:** `admin@novoyahope.com`
- **Пароль:** `SecureAdmin123!`

### Важно:
⚠️ **ОБЯЗАТЕЛЬНО измените пароль после первого входа!**

### Как это работает:
1. При запуске приложения вызывается `DbInitializer.Initialize()`
2. Система проверяет, есть ли уже пользователи с ролью "Administrator"
3. Если администраторов нет, создается пользователь с указанными выше данными
4. Если администратор уже существует, новый не создается

---

## Способ 2: Создание через админ-панель

Если у вас уже есть доступ к админ-панели, вы можете назначить роль администратора любому пользователю.

### Шаги:
1. Войдите в систему как администратор
2. Перейдите в **Админ-панель** (ссылка в меню навигации)
3. Откройте раздел **"Управление пользователями"**
4. Найдите нужного пользователя в списке
5. Нажмите на кнопку с иконкой щита (🛡️) рядом с пользователем
6. Пользователю будет назначена роль "Administrator"

### Ограничения:
- Нельзя удалить роль администратора у последнего администратора в системе
- Это защищает от случайной потери доступа к админ-панели

---

## Способ 3: Создание через код (для разработчиков)

Если нужно создать администратора программно, можно использовать следующий код:

### Пример кода:

```csharp
using Microsoft.AspNetCore.Identity;
using NovoyaHope.Models;

// В контроллере или сервисе
public async Task CreateAdminUser(
    UserManager<ApplicationUser> userManager,
    RoleManager<IdentityRole> roleManager)
{
    // 1. Убедитесь, что роль "Administrator" существует
    if (!await roleManager.RoleExistsAsync("Administrator"))
    {
        await roleManager.CreateAsync(new IdentityRole("Administrator"));
    }

    // 2. Создайте пользователя
    var adminUser = new ApplicationUser
    {
        UserName = "your-admin@example.com",
        Email = "your-admin@example.com",
        EmailConfirmed = true
    };

    var result = await userManager.CreateAsync(adminUser, "YourSecurePassword123!");

    if (result.Succeeded)
    {
        // 3. Назначьте роль администратора
        await userManager.AddToRoleAsync(adminUser, "Administrator");
    }
}
```

---

## Способ 4: Создание через SQL (Продвинутый метод)

Если у вас есть прямой доступ к базе данных, можно создать администратора через SQL запросы.

### Внимание:
⚠️ Этот метод требует знания структуры базы данных Identity и может быть небезопасным.

### Пример SQL (для SQL Server):

```sql
-- 1. Создайте пользователя (замените значения)
DECLARE @UserId NVARCHAR(450) = NEWID();
DECLARE @Email NVARCHAR(256) = 'admin@example.com';
DECLARE @PasswordHash NVARCHAR(MAX); -- Нужно сгенерировать хеш пароля

-- Вставка в таблицу User
INSERT INTO [Identity].[User] (Id, UserName, NormalizedUserName, Email, NormalizedEmail, 
                                EmailConfirmed, PasswordHash, SecurityStamp, ConcurrencyStamp)
VALUES (@UserId, @Email, UPPER(@Email), @Email, UPPER(@Email), 1, @PasswordHash, 
        NEWID(), NEWID());

-- 2. Получите ID роли Administrator
DECLARE @RoleId NVARCHAR(450);
SELECT @RoleId = Id FROM [Identity].[Role] WHERE NormalizedName = 'ADMINISTRATOR';

-- 3. Назначьте роль пользователю
INSERT INTO [Identity].[UserRoles] (UserId, RoleId)
VALUES (@UserId, @RoleId);
```

**Примечание:** Для генерации правильного хеша пароля лучше использовать метод `PasswordHasher` из ASP.NET Core Identity.

---

## Проверка прав администратора

После создания администратора вы можете проверить его права:

1. Войдите в систему с учетными данными администратора
2. В меню навигации должна появиться ссылка **"Админ-панель"**
3. Перейдите по ссылке - вы должны увидеть дашборд администратора
4. Доступны разделы:
   - Управление пользователями
   - Управление опросами
   - Управление шаблонами

---

## Безопасность

### Рекомендации:

1. **Измените пароль по умолчанию** сразу после первого входа
2. **Используйте сложные пароли** (минимум 8 символов, буквы, цифры, спецсимволы)
3. **Не делитесь учетными данными** администратора
4. **Регулярно проверяйте** список администраторов в системе
5. **Удаляйте неиспользуемые** администраторские аккаунты

### Изменение пароля администратора по умолчанию:

1. Войдите в систему с учетными данными по умолчанию
2. Перейдите в **Профиль** (меню пользователя)
3. Используйте функцию **"Изменить пароль"**
4. Введите новый надежный пароль

---

## Устранение проблем

### Проблема: Администратор не создается автоматически

**Решение:**
1. Проверьте логи приложения на наличие ошибок
2. Убедитесь, что база данных создана и доступна
3. Проверьте, что миграции применены: `dotnet ef database update`
4. Убедитесь, что в базе данных нет пользователей с ролью "Administrator"

### Проблема: Не могу войти как администратор

**Решение:**
1. Проверьте правильность email и пароля
2. Убедитесь, что пользователь имеет роль "Administrator"
3. Проверьте, что `EmailConfirmed = true`
4. Попробуйте сбросить пароль через админ-панель (если есть другой администратор)

### Проблема: Нет доступа к админ-панели

**Решение:**
1. Убедитесь, что вы вошли в систему
2. Проверьте, что у вашего пользователя есть роль "Administrator"
3. Очистите кеш браузера и попробуйте снова
4. Проверьте консоль браузера на наличие ошибок JavaScript

---

## Дополнительная информация

- Роли создаются автоматически при первом запуске: `Administrator` и `User`
- Все новые пользователи получают роль `User` по умолчанию
- Администратор может управлять ролями других пользователей через админ-панель
- В системе должен быть хотя бы один администратор

---

**Дата создания:** 2025
**Версия:** 1.0

