# Документация структуры проекта - NovoyaHope

## Содержание

1. [Обзор](#обзор)
2. [Корневая структура](#корневая-структура)
3. [Детальное описание папок](#детальное-описание-папок)
4. [Описание файлов](#описание-файлов)
5. [Зависимости и пакеты](#зависимости-и-пакеты)
6. [Архитектурные принципы](#архитектурные-принципы)

---

## Обзор

Проект NovoyaHope построен на архитектуре ASP.NET Core MVC с использованием многослойной структуры. Проект следует принципам Clean Architecture и разделения ответственности.

### Технологический стек

- **Платформа**: .NET 8.0
- **Фреймворк**: ASP.NET Core MVC
- **ORM**: Entity Framework Core 8.0
- **База данных**: Microsoft SQL Server
- **Аутентификация**: ASP.NET Core Identity
- **Frontend**: Razor Views, JavaScript, CSS
- **Библиотеки**: Bootstrap 5, jQuery, Font Awesome

---

## Корневая структура

```
NovoyaHope/
├── Controllers/          # Контроллеры (серверная логика HTTP запросов)
├── Models/              # Модели данных и ViewModels
├── Views/               # Razor представления
├── Services/            # Сервисы бизнес-логики
├── Data/                # Контекст базы данных и инициализация
├── Helpers/             # Вспомогательные классы
├── Migrations/          # Миграции Entity Framework
├── wwwroot/             # Статические файлы (CSS, JS, изображения)
├── Properties/          # Настройки проекта
├── bin/                 # Скомпилированные файлы (генерируется)
├── obj/                 # Временные файлы компиляции (генерируется)
├── Program.cs           # Точка входа приложения
├── NovoyaHope.csproj    # Файл проекта
└── appsettings.json     # Конфигурация приложения
```

---

## Детальное описание папок

### 📁 Controllers/

**Назначение**: Обработка HTTP запросов и координация бизнес-логики

**Содержимое**:
- `AccountController.cs` - Аутентификация, регистрация, управление профилем
- `AdminController.cs` - Административная панель
- `HomeController.cs` - Публичные страницы (главная, about, help и т.д.)
- `PublicController.cs` - Публичный доступ к опросам
- `SurveyController.cs` - Управление опросами (создание, редактирование, результаты)

**Принципы**:
- Контроллеры тонкие - делегируют бизнес-логику сервисам
- Обрабатывают валидацию входных данных
- Управляют авторизацией и правами доступа
- Формируют ViewModels для представлений

---

### 📁 Models/

**Назначение**: Определение структуры данных, сущностей и ViewModels

**Содержимое**:

#### Основные модели (Entity Models):
- `ApplicationUser.cs` - Расширенная модель пользователя (Identity)
- `Survey.cs` - Модель опроса с настройками и связями
- `Question.cs` - Модель вопроса с enum типов вопросов
- `AnswerOption.cs` - Модель варианта ответа
- `SurveyResponse.cs` - Модель ответа на опрос (один проход)
- `UserAnswer.cs` - Модель конкретного ответа на вопрос
- `Section.cs` - Модель раздела опроса
- `Media.cs` - Модель медиа-элемента (изображение/видео)
- `SurveyTemplate.cs` - Модель шаблона опроса
- `Answer.cs` - (Устаревшая модель, возможно не используется)

#### ViewModels (Models/ViewModels/):
- `SurveyViewModels.cs` - ViewModels для опросов (SaveSurveyViewModel, SurveyResultsViewModel и т.д.)
- `SurveyConstructorViewModel.cs` - ViewModel для конструктора
- `PassSurveyViewModels.cs` - ViewModels для прохождения опросов
- `ResponseViewModels.cs` - ViewModels для отображения результатов
- `AdminViewModels.cs` - ViewModels для админ-панели
- `LoginViewModel.cs` - ViewModel входа
- `RegisterViewModel.cs` - ViewModel регистрации
- `ProfileViewModel.cs` - ViewModel профиля пользователя
- `ErrorViewModel.cs` - ViewModel ошибок

**Принципы**:
- Разделение Entity Models и ViewModels
- ViewModels для передачи данных в представления
- Enum типы для ограниченных значений

---

### 📁 Views/

**Назначение**: Razor представления (UI слой)

**Структура**:

```
Views/
├── Shared/              # Общие компоненты
│   ├── _Layout.cshtml           # Основной layout
│   ├── _Layout.cshtml.css       # Стили layout
│   ├── _LoginPartial.cshtml     # Частичное представление входа
│   ├── _ViewImports.cshtml      # Глобальные imports
│   ├── _ViewStart.cshtml        # Начальные настройки
│   ├── _ValidationScriptsPartial.cshtml  # Скрипты валидации
│   └── Error.cshtml             # Страница ошибки
│
├── Home/                # Публичные страницы
│   ├── Index.cshtml             # Главная страница
│   ├── About.cshtml             # О нас
│   ├── Features.cshtml          # Возможности
│   ├── Pricing.cshtml           # Тарифы
│   ├── Help.cshtml              # Помощь
│   ├── Documentation.cshtml     # Документация
│   ├── Blog.cshtml              # Блог
│   ├── Contact.cshtml           # Контакты
│   ├── Privacy.cshtml           # Политика конфиденциальности
│   ├── Terms.cshtml             # Условия использования
│   └── Cookies.cshtml           # Cookie
│
├── Account/             # Аутентификация и профиль
│   ├── Login.cshtml             # Страница входа
│   ├── Register.cshtml          # Страница регистрации
│   ├── Profile.cshtml           # Профиль пользователя
│   └── Settings.cshtml          # Настройки
│
├── Survey/              # Управление опросами
│   ├── Index.cshtml             # Список опросов
│   ├── Constructor.cshtml       # Конструктор опросов
│   ├── Preview.cshtml           # Предпросмотр опроса
│   ├── Results.cshtml           # Результаты опроса
│   └── Templates.cshtml         # Шаблоны опросов
│
└── Public/              # Публичный доступ
    ├── Pass.cshtml              # Прохождение опроса
    └── ThankYou.cshtml          # Страница благодарности
```

**Принципы**:
- Разделение по контроллерам
- Переиспользование компонентов через частичные представления
- Использование Layout для единообразия

---

### 📁 Services/

**Назначение**: Бизнес-логика приложения

**Содержимое**:
- `ISurveyService.cs` - Интерфейс сервиса опросов
- `SurveyService.cs` - Реализация сервиса опросов

**Функциональность SurveyService**:
- Сохранение и обновление опросов
- Синхронизация вопросов, вариантов ответов, разделов, медиа
- Получение опросов для редактирования
- Управление статусом публикации
- Валидация опросов
- Удаление опросов

**Принципы**:
- Инкапсуляция бизнес-логики
- Разделение интерфейса и реализации
- Регистрация через Dependency Injection (Scoped)

---

### 📁 Data/

**Назначение**: Работа с базой данных

**Содержимое**:
- `ApplicationDbContext.cs` - Контекст Entity Framework
- `DbInitializer.cs` - Инициализация базы данных

**ApplicationDbContext**:
- Определяет DbSet'ы для всех сущностей
- Настраивает связи между сущностями
- Определяет каскадные удаления
- Настраивает схему Identity

**DbInitializer**:
- Создает роли (Administrator, User)
- Создает администратора по умолчанию
- Вызывается при старте приложения

---

### 📁 Helpers/

**Назначение**: Переиспользуемые вспомогательные функции

**Содержимое**:
- `ImageHelper.cs` - Работа с изображениями
  - Валидация изображений
  - Сохранение изображений профиля
  - Сохранение медиа-изображений
  - Удаление изображений
  - Форматирование размера файла

- `ValidationHelper.cs` - Валидация данных
  - Валидация Email
  - Валидация телефона
  - Форматирование телефона
  - Проверка надежности пароля
  - Валидация имени/фамилии
  - Санитизация строк
  - Проверка длины и допустимых символов

**Принципы**:
- Статические классы с утилитарными методами
- Переиспользуемость без состояния

---

### 📁 Migrations/

**Назначение**: Миграции базы данных Entity Framework

**Содержимое**:
- Миграции создаются автоматически при выполнении `dotnet ef migrations add`
- Каждая миграция состоит из:
  - `{timestamp}_{name}.cs` - Миграция
  - `{timestamp}_{name}.Designer.cs` - Метаданные миграции
- `ApplicationDbContextModelSnapshot.cs` - Текущее состояние модели

**Важные миграции**:
- `20251127190705_db3.cs` - Базовая структура
- `20251130155523_AddIsOtherToAnswerOption.cs` - Добавление поля IsOther

**Принципы**:
- Миграции версионируются по времени
- Не редактировать созданные миграции вручную

---

### 📁 wwwroot/

**Назначение**: Статические файлы (CSS, JavaScript, изображения, библиотеки)

**Структура**:

```
wwwroot/
├── css/                 # Кастомные стили
│   ├── layout.css           # Стили навигации и layout
│   ├── site.css             # Базовые стили сайта
│   ├── home.css             # Стили главной страницы
│   ├── surveys.css          # Стили страницы опросов
│   ├── constructor.css      # Стили конструктора
│   ├── admin.css            # Стили админ-панели
│   └── templates.css        # Стили страницы шаблонов
│
├── js/                  # Кастомные скрипты
│   ├── layout.js            # Скрипты навигации
│   ├── site.js              # Общие скрипты
│   └── constructor.js       # Скрипты конструктора
│
├── lib/                 # Внешние библиотеки
│   ├── bootstrap/           # Bootstrap 5
│   │   └── dist/
│   │       ├── css/         # CSS файлы Bootstrap
│   │       └── js/          # JS файлы Bootstrap
│   ├── jquery/              # jQuery
│   │   └── dist/
│   ├── jquery-validation/   # jQuery Validation
│   │   └── dist/
│   └── jquery-validation-unobtrusive/  # Unobtrusive Validation
│
├── uploads/             # Загруженные пользователями файлы
│   └── profiles/            # Фотографии профилей
│       └── {userId}_{guid}.jpg
│
└── favicon.ico          # Иконка сайта
```

**Примечание**: 
- `uploads/` - создается динамически при загрузке файлов
- Файлы в `lib/` управляются через NuGet или вручную

---

### 📁 Properties/

**Назначение**: Настройки проекта и конфигурация

**Содержимое**:
- `launchSettings.json` - Настройки запуска приложения
  - Профили запуска (http, https, IIS Express)
  - URL и порты
  - Переменные окружения

---

### 📁 bin/ и obj/

**Назначение**: Сгенерированные файлы компиляции

**Содержимое**:
- `bin/` - Скомпилированные DLL и исполняемые файлы
- `obj/` - Временные файлы компиляции, метаданные

**Примечание**: Эти папки не коммитятся в Git (в .gitignore)

---

## Описание файлов

### Корневые файлы проекта

#### Program.cs

**Назначение**: Точка входа приложения, конфигурация сервисов

**Содержит**:
- Конфигурация Entity Framework
- Настройка Identity
- Регистрация сервисов
- Конфигурация middleware (Authentication, Authorization, Routing)
- Инициализация базы данных
- Настройка маршрутизации

**Важные настройки**:
- Глобальная политика авторизации (RequireAuthenticatedUser)
- JSON сериализация с поддержкой enum как строк
- Обработка ошибок для Development/Production

#### NovoyaHope.csproj

**Назначение**: Файл проекта .NET

**Содержит**:
- Target Framework: net8.0
- Nullable reference types: enabled
- Package References:
  - Microsoft.AspNetCore.* (8.0.0)
  - Microsoft.EntityFrameworkCore.* (8.0.0)
  - Microsoft.AspNetCore.Identity.* (8.0.0)

#### appsettings.json

**Назначение**: Конфигурация приложения

**Содержит**:
- ConnectionStrings (строка подключения к БД)
- Logging (уровни логирования)
- AllowedHosts

#### appsettings.Development.json

**Назначение**: Конфигурация для окружения Development

**Содержит**: Переопределения настроек для разработки

---

### Файлы конфигурации и скрипты

#### SQL скрипты

- `add-profile-fields.sql` - Добавление полей профиля в БД
- `FixTextAnswerColumn.sql` - Исправление колонки TextAnswer

#### Batch скрипты

- `apply-migration.bat` - Скрипт для применения миграций

---

### Документация

Все файлы документации находятся в корне проекта:

#### Основная документация

- `DATABASE_DOCUMENTATION.md` - Документация базы данных
- `SERVER_LOGIC_DOCUMENTATION.md` - Документация серверной логики
- `UI_DOCUMENTATION.md` - Документация интерфейса
- `USER_INTERACTION_DOCUMENTATION.md` - Документация взаимодействия с пользователем
- `PROJECT_STRUCTURE_DOCUMENTATION.md` - Эта документация

#### Руководства

- `QUICK_START.md` - Быстрый старт
- `PROJECT_STATUS.md` - Статус проекта
- `SYSTEM_DESIGN.md` - Дизайн системы
- `USER_PROFILE_GUIDE.md` - Руководство по профилю пользователя
- `ADMIN_CREATION_GUIDE.md` - Руководство по созданию администратора
- `README_CONSTRUCTOR.md` - Руководство по конструктору

#### Техническая документация

- `UPGRADE_INSTRUCTIONS.md` - Инструкции по обновлению
- `MANUAL_MIGRATION_GUIDE.md` - Руководство по миграциям
- `FIX_ERRORS_GUIDE.md` - Руководство по исправлению ошибок
- `RECOMMENDATIONS.md` - Рекомендации по развитию

---

## Зависимости и пакеты

### NuGet пакеты

#### ASP.NET Core

```xml
Microsoft.AspNetCore.Authorization (8.0.0)
Microsoft.AspNetCore.Diagnostics.EntityFrameworkCore (8.0.0)
Microsoft.AspNetCore.Identity.EntityFrameworkCore (8.0.0)
Microsoft.AspNetCore.Identity.UI (8.0.0)
Microsoft.AspNetCore.Mvc.Razor.RuntimeCompilation (8.0.0)
```

#### Entity Framework Core

```xml
Microsoft.EntityFrameworkCore.Design (8.0.0)
Microsoft.EntityFrameworkCore.SqlServer (8.0.0)
Microsoft.EntityFrameworkCore.Tools (8.0.0)
```

#### Инструменты разработки

```xml
Microsoft.VisualStudio.Web.CodeGeneration.Design (8.0.0)
```

### Frontend библиотеки

#### Bootstrap 5
- Расположение: `wwwroot/lib/bootstrap/`
- Использование: Частичное (основные компоненты)

#### jQuery
- Версия: Последняя стабильная
- Расположение: `wwwroot/lib/jquery/`
- Использование: Для DOM манипуляций и AJAX

#### jQuery Validation
- Расположение: `wwwroot/lib/jquery-validation/`
- Использование: Валидация форм на клиенте

#### jQuery Validation Unobtrusive
- Расположение: `wwwroot/lib/jquery-validation-unobtrusive/`
- Использование: Интеграция с ASP.NET Core валидацией

#### Font Awesome
- Версия: 6.4.0
- Источник: CDN (в _Layout.cshtml)
- Использование: Иконки по всему приложению

---

## Архитектурные принципы

### Разделение ответственности

```
┌─────────────────────────────────────────┐
│           Presentation Layer            │
│  Controllers → Views → ViewModels       │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│           Business Layer                │
│           Services                      │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│            Data Layer                   │
│  ApplicationDbContext → Database        │
└─────────────────────────────────────────┘
```

### Принципы проектирования

1. **Separation of Concerns** - Каждый класс имеет одну ответственность
2. **Dependency Injection** - Зависимости внедряются через конструкторы
3. **Repository Pattern** - Entity Framework выступает как репозиторий
4. **Service Layer Pattern** - Бизнес-логика в сервисах
5. **ViewModel Pattern** - Отдельные модели для представлений

### Паттерны проектирования

- **MVC (Model-View-Controller)** - Основная архитектура
- **Dependency Injection** - Внедрение зависимостей
- **Repository Pattern** - Доступ к данным через DbContext
- **Service Pattern** - Бизнес-логика в сервисах
- **ViewModel Pattern** - Отделение данных от представления

---

## Namespace структура

```
NovoyaHope
├── Controllers          # NovoyaHope.Controllers
│   ├── AccountController
│   ├── AdminController
│   ├── HomeController
│   ├── PublicController
│   └── SurveyController
│
├── Models              # NovoyaHope.Models
│   ├── ApplicationUser
│   ├── Survey
│   ├── Question
│   └── ViewModels      # NovoyaHope.Models.ViewModels
│
├── Services            # NovoyaHope.Services
│   ├── ISurveyService
│   └── SurveyService
│
├── Data                # NovoyaHope.Data
│   ├── ApplicationDbContext
│   └── DbInitializer
│
└── Helpers             # NovoyaHope.Helpers
    ├── ImageHelper
    └── ValidationHelper
```

---

## Структура файлов по типам

### C# файлы (.cs)

```
По назначению:
├── Controllers/        # Обработка HTTP запросов
├── Models/            # Модели данных
├── Services/          # Бизнес-логика
├── Data/              # Доступ к данным
└── Helpers/           # Утилиты
```

### Razor файлы (.cshtml)

```
Views/
├── Shared/            # Общие компоненты
├── Home/              # Публичные страницы
├── Account/           # Аутентификация
├── Survey/            # Управление опросами
└── Public/            # Публичный доступ
```

### CSS файлы (.css)

```
wwwroot/css/
├── layout.css         # Основной layout
├── site.css           # Базовые стили
├── home.css           # Главная страница
├── surveys.css        # Список опросов
├── constructor.css    # Конструктор
├── admin.css          # Админ-панель
└── templates.css      # Шаблоны
```

### JavaScript файлы (.js)

```
wwwroot/js/
├── layout.js          # Навигация и общие функции
├── site.js            # Общие скрипты
└── constructor.js     # Логика конструктора
```

---

## Соглашения об именовании

### Классы

- **Контроллеры**: `{Name}Controller` (например, `SurveyController`)
- **Сервисы**: `{Name}Service` (например, `SurveyService`)
- **Модели**: Имя сущности (например, `Survey`, `Question`)
- **ViewModels**: `{Purpose}ViewModel` (например, `SaveSurveyViewModel`)
- **Helpers**: `{Purpose}Helper` (например, `ImageHelper`)

### Файлы

- **Контроллеры**: `{Name}Controller.cs`
- **Модели**: `{ModelName}.cs`
- **Views**: `{ActionName}.cshtml`
- **Стили**: `{page}.css` (например, `home.css`)
- **Скрипты**: `{page}.js` (например, `constructor.js`)

### Папки

- Множественное число для коллекций (Controllers, Models, Views)
- Единственное число для абстракций (Data, Services)

---

## Важные конфигурационные файлы

### Connection String

**Расположение**: `appsettings.json`

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=h9ise;Database=NovoyaHopeDb;..."
  }
}
```

### Launch Settings

**Расположение**: `Properties/launchSettings.json`

**Профили**:
- `http`: http://localhost:5017
- `https`: https://localhost:7285;http://localhost:5017
- `IIS Express`: http://localhost:5615

---

## Генерируемые файлы и папки

### Не включать в систему контроля версий

```
bin/
obj/
wwwroot/uploads/
*.user
*.suo
*.cache
```

### Включать в систему контроля версий

```
Migrations/
wwwroot/lib/
*.csproj
*.json
*.cs
*.cshtml
*.css
*.js
*.md
```

---

## Структура базы данных

См. подробную документацию: `DATABASE_DOCUMENTATION.md`

**Основные таблицы**:
- Identity.User (ApplicationUser)
- Surveys
- Questions
- AnswerOptions
- SurveyResponses
- UserAnswers
- Sections
- Media
- SurveyTemplates

---

## Взаимосвязи компонентов

### Поток данных

```
HTTP Request
    ↓
Controller (обработка запроса)
    ↓
Service (бизнес-логика)
    ↓
ApplicationDbContext (доступ к данным)
    ↓
Database (SQL Server)
    ↓
ApplicationDbContext (результаты)
    ↓
Service (обработка данных)
    ↓
Controller (формирование ответа)
    ↓
View (Razor представление)
    ↓
HTTP Response
```

### Зависимости

```
Controllers
    ├── Services
    ├── Models
    ├── Helpers
    └── Data (ApplicationDbContext)

Services
    └── Data (ApplicationDbContext)

Helpers
    └── (независимые утилиты)

Data
    └── Models
```

---

## Особенности структуры

### 1. Многослойная архитектура

Проект четко разделен на слои:
- **Presentation** (Controllers + Views)
- **Business** (Services)
- **Data** (DbContext + Models)

### 2. Dependency Injection

Все зависимости регистрируются в `Program.cs`:
- `AddScoped<ISurveyService, SurveyService>()`
- `AddDbContext<ApplicationDbContext>()`
- Identity сервисы

### 3. Конвенции ASP.NET Core

- Контроллеры в папке `Controllers/`
- Views в папке `Views/{Controller}/`
- Статические файлы в `wwwroot/`
- Модели в папке `Models/`

### 4. Entity Framework Migrations

- Миграции в папке `Migrations/`
- Автоматическая генерация при изменениях моделей
- Применение через `dotnet ef database update`

---

## Рекомендации по расширению

### Добавление нового контроллера

1. Создать файл `Controllers/{Name}Controller.cs`
2. Наследовать от `Controller`
3. Добавить методы действий
4. Создать папку `Views/{Name}/` с представлениями

### Добавление нового сервиса

1. Создать интерфейс `I{Name}Service.cs` в `Services/`
2. Создать реализацию `{Name}Service.cs`
3. Зарегистрировать в `Program.cs`: `AddScoped<I{Name}Service, {Name}Service>()`

### Добавление новой модели

1. Создать файл `Models/{ModelName}.cs`
2. Добавить DbSet в `ApplicationDbContext`
3. Создать миграцию: `dotnet ef migrations add {Name}`

### Добавление ViewModel

1. Создать файл в `Models/ViewModels/{Purpose}ViewModel.cs`
2. Определить свойства для передачи данных в представление
3. Использовать в контроллере и представлении

---

## Размеры проекта (приблизительно)

```
Файлы кода:
- Controllers: 5 файлов
- Models: 10+ файлов
- Services: 2 файла
- Data: 2 файла
- Helpers: 2 файла
- Views: 20+ файлов

Строки кода (приблизительно):
- C#: ~5000+ строк
- Razor: ~3000+ строк
- CSS: ~2000+ строк
- JavaScript: ~1500+ строк
```

---

## Версия документации

- **Версия**: 1.0
- **Дата**: 2025
- **Автор**: NovoyaHope Development Team
- **Последнее обновление**: Соответствует коду на момент создания документации

---

## Связанная документация

- `DATABASE_DOCUMENTATION.md` - Подробная информация о базе данных
- `SERVER_LOGIC_DOCUMENTATION.md` - Описание серверной логики
- `UI_DOCUMENTATION.md` - Документация интерфейса
- `USER_INTERACTION_DOCUMENTATION.md` - Пользовательские сценарии
- `SYSTEM_DESIGN.md` - Дизайн системы

---

## Примечания

### Важные моменты

1. **Папка "Новая папка"** в .csproj - можно удалить (пустая папка-заглушка)

2. **Модель Answer.cs** - возможно устаревшая, используется UserAnswer вместо нее

3. **Модель Option.cs** - возможно дублирует AnswerOption

4. **Миграции** - не редактировать вручную после создания

5. **wwwroot/uploads/** - создается автоматически, не коммитить пользовательские файлы

6. **bin/ и obj/** - не коммитить в систему контроля версий

---

## Быстрая навигация

### Найти код для...

- **Аутентификации** → `Controllers/AccountController.cs`
- **Создания опроса** → `Controllers/SurveyController.cs` → `Edit()`
- **Бизнес-логики опросов** → `Services/SurveyService.cs`
- **Моделей данных** → `Models/`
- **Представлений** → `Views/{Controller}/`
- **Стилей** → `wwwroot/css/`
- **Скриптов** → `wwwroot/js/`
- **Конфигурации БД** → `Data/ApplicationDbContext.cs`
- **Валидации** → `Helpers/ValidationHelper.cs`
- **Работы с файлами** → `Helpers/ImageHelper.cs`

---

## Заключение

Проект NovoyaHope имеет четкую структуру, следующую лучшим практикам ASP.NET Core MVC. Разделение на слои обеспечивает поддерживаемость и расширяемость кода. Все компоненты имеют свои четко определенные роли и ответственность.

