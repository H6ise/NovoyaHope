# Инструкция по исправлению ошибок компиляции

## ✅ Что было исправлено

### 1. Модель ApplicationUser
- Добавлен конструктор с инициализацией коллекций
- Все поля профиля на месте: FirstName, LastName, ProfileImagePath, ShowPhoneToPublic

### 2. ViewModels
- Исправлены предупреждения о non-nullable свойствах
- Добавлена инициализация значениями по умолчанию

### 3. Helper классы
- ImageHelper - упрощен, не требует сторонних библиотек
- ValidationHelper - готов к использованию

## 🔧 Шаги для устранения ошибок

### Шаг 1: Очистка и пересборка проекта

Выполните следующие команды в терминале:

```bash
# Очистка проекта
dotnet clean

# Удаление папки с артефактами сборки
Remove-Item -Recurse -Force bin, obj

# Восстановление пакетов
dotnet restore

# Пересборка проекта
dotnet build
```

### Шаг 2: Применение миграции к базе данных

**ВАЖНО:** Перед применением миграции убедитесь, что в `appsettings.json` правильно настроена строка подключения к базе данных.

```bash
# Применить все миграции
dotnet ef database update
```

Если возникнут проблемы с миграцией:

```bash
# Откатить последнюю миграцию
dotnet ef database update 20251125195020_db1

# Удалить миграцию AddUserProfileFields
dotnet ef migrations remove

# Создать миграцию заново
dotnet ef migrations add AddUserProfileFields

# Применить миграцию
dotnet ef database update
```

### Шаг 3: Проверка базы данных

После применения миграции проверьте, что в таблице `User` (схема `Identity`) появились новые столбцы:
- FirstName (nvarchar(50), nullable)
- LastName (nvarchar(50), nullable)
- ProfileImagePath (nvarchar(500), nullable)
- ShowPhoneToPublic (bit, NOT NULL, default: 0)

### Шаг 4: Проверка compilation

```bash
# Пересобрать проект
dotnet build

# Если есть ошибки, посмотрите детали
dotnet build --verbosity detailed
```

### Шаг 5: Запуск приложения

```bash
# Запустить приложение
dotnet run
```

Или через Visual Studio:
- Нажмите F5 или кнопку "Start Debugging"

## 🐛 Типичные ошибки и их решение

### Ошибка: "ApplicationUser не содержит определения [свойство]"

**Причина:** Проект не пересобран после изменения модели

**Решение:**
1. Закройте Visual Studio (если открыт)
2. Удалите папки `bin` и `obj`
3. Откройте проект снова
4. Выполните `dotnet build`

### Ошибка: "Миграция уже применена"

**Причина:** База данных уже содержит эту миграцию

**Решение:**
Проверьте таблицу `__EFMigrationsHistory` в базе данных. Если миграция `20251127000000_AddUserProfileFields` уже там есть, значит она применена.

### Ошибка: "Invalid column name 'FirstName'"

**Причина:** Миграция не применена к базе данных

**Решение:**
```bash
dotnet ef database update
```

### Ошибка: "Could not load file or assembly"

**Причина:** Кэш NuGet пакетов поврежден

**Решение:**
```bash
dotnet nuget locals all --clear
dotnet restore
dotnet build
```

## 📋 Проверочный список

После выполнения всех шагов проверьте:

- [ ] Проект успешно компилируется без ошибок
- [ ] База данных обновлена (есть новые столбцы в таблице User)
- [ ] Приложение запускается без ошибок
- [ ] В навигационной панели отображается аватар пользователя
- [ ] При клике на аватар открывается выпадающее меню
- [ ] При клике на email открывается модальное окно с информацией
- [ ] Страница профиля (/Account/Profile) открывается
- [ ] Можно загрузить фото профиля
- [ ] Можно изменить пароль

## 🆘 Если ничего не помогло

1. **Создайте резервную копию базы данных**

2. **Полностью удалите базу данных и создайте заново:**
```bash
dotnet ef database drop
dotnet ef database update
```

3. **Проверьте версии пакетов:**
```bash
dotnet list package
```

Все пакеты должны быть версии 8.0.0

4. **Проверьте целостность файлов:**
- `Models/ApplicationUser.cs` - должен содержать все новые поля
- `Data/ApplicationDbContext.cs` - должен использовать `ApplicationUser`
- `Migrations/` - должна быть миграция `AddUserProfileFields`

5. **Создайте новую миграцию с другим именем:**
```bash
dotnet ef migrations add UpdateUserProfile
dotnet ef database update
```

## 📞 Дополнительная помощь

Если проблема не решается:

1. Откройте файл с ошибкой в редакторе
2. Проверьте наличие директивы `using NovoyaHope.Models;`
3. Убедитесь, что ApplicationUser импортирован корректно
4. Проверьте namespace в файлах

## ✨ После успешного исправления

Протестируйте следующие сценарии:

1. **Регистрация нового пользователя:**
   - Зарегистрируйтесь
   - Перейдите в профиль
   - Заполните все поля
   - Загрузите фото

2. **Работа с профилем:**
   - Измените имя и фамилию
   - Добавьте телефон
   - Настройте видимость телефона
   - Измените пароль

3. **Модальное окно:**
   - Кликните на аватар
   - Кликните на email
   - Проверьте отображение всех данных
   - Попробуйте скопировать email/телефон

---

**Дата создания:** 27 ноября 2025  
**Версия:** 1.0

