# 📋 Документация по реализации типов вопросов в системе опросов

## Обзор

Система NovoyaHope поддерживает **12 различных типов вопросов** для создания гибких и функциональных опросов. Каждый тип вопроса имеет свою специфику реализации, отображения и анализа результатов.

---

## 📊 Типы вопросов и их реализация

### 1. 📝 ShortText - Текст (строка)

**Описание:** Короткий однострочный текстовый ответ.

**Использование:**
- Имя, фамилия
- Email адрес
- Номер телефона
- Краткие ответы

**Реализация:**
- **Frontend (Pass.cshtml):** `<input type="text">`
- **Backend:** Сохраняется в `UserAnswer.TextAnswer`
- **Аналитика:** Отображается список всех текстовых ответов

**Код реализации:**
```csharp
case QuestionType.ShortText:
    <input type="text" class="input-line"
           name="Answers[@question.Id].TextAnswer"
           placeholder="Краткий ответ"
           @(question.IsRequired ? "required" : "")>
    break;
```

**Аналитика:**
- Тип визуализации: Список ответов
- График: Не требуется
- Метрики: Количество ответов, уникальные значения

---

### 2. 📄 ParagraphText - Текст (абзац)

**Описание:** Многострочный текстовый ответ для развернутых комментариев.

**Использование:**
- Комментарии и отзывы
- Описание проблемы
- Развернутые ответы

**Реализация:**
- **Frontend (Pass.cshtml):** `<textarea rows="4">`
- **Backend:** Сохраняется в `UserAnswer.TextAnswer`
- **Аналитика:** Отображается список всех текстовых ответов с прокруткой

**Код реализации:**
```csharp
case QuestionType.ParagraphText:
    <textarea class="input-line"
               name="Answers[@question.Id].TextAnswer"
               placeholder="Развернутый ответ"
               rows="4"
               @(question.IsRequired ? "required" : "")></textarea>
    break;
```

**Аналитика:**
- Тип визуализации: Список ответов с прокруткой
- График: Не требуется
- Метрики: Количество ответов, средняя длина ответа

---

### 3. ⭕ SingleChoice - Один из списка (Radio)

**Описание:** Выбор одного варианта из предложенных (радио-кнопки).

**Использование:**
- Пол (Мужской/Женский)
- Возрастная категория
- Категория продукта
- Статус

**Реализация:**
- **Frontend (Pass.cshtml):** `<input type="radio">`
- **Backend:** Сохраняется в `UserAnswer.SelectedOptionId`
- **Аналитика:** Круговая диаграмма (Pie Chart) + процентное распределение

**Код реализации:**
```csharp
case QuestionType.SingleChoice:
    @foreach (var option in question.Options)
    {
        <label class="radio-option">
            <input type="radio"
                   name="Answers[@question.Id].SelectedOptionIds"
                   value="@option.Id"
                   @(question.IsRequired ? "required" : "")>
            <span class="option-text">@option.Text</span>
        </label>
    }
    break;
```

**Аналитика:**
- Тип визуализации: **Круговая диаграмма (Pie Chart)**
- График: Chart.js - Pie Chart
- Метрики: Количество ответов по каждому варианту, процентное соотношение
- Цвета: Зеленая палитра с градиентами

---

### 4. ☑️ MultipleChoice - Несколько из списка (Checkbox)

**Описание:** Выбор нескольких вариантов из предложенных (чекбоксы).

**Использование:**
- Интересы и хобби
- Навыки и компетенции
- Предпочтения в продуктах
- Множественный выбор категорий

**Реализация:**
- **Frontend (Pass.cshtml):** `<input type="checkbox">`
- **Backend:** Сохраняется несколько записей в `UserAnswer` с разными `SelectedOptionId`
- **Аналитика:** Столбчатая диаграмма (Bar Chart) + процентное распределение

**Код реализации:**
```csharp
case QuestionType.MultipleChoice:
    @foreach (var option in question.Options)
    {
        <label class="checkbox-option">
            <input type="checkbox"
                   name="Answers[@question.Id].SelectedOptionIds"
                   value="@option.Id">
            <span class="option-text">@option.Text</span>
        </label>
    }
    break;
```

**Аналитика:**
- Тип визуализации: **Столбчатая диаграмма (Bar Chart)**
- График: Chart.js - Bar Chart
- Метрики: Количество выборов каждого варианта, процент от общего числа ответов
- Особенность: Один респондент может выбрать несколько вариантов

---

### 5. 🔽 Dropdown - Раскрывающийся список

**Описание:** Выбор одного варианта из выпадающего списка.

**Использование:**
- Страна, город
- Большой список вариантов
- Категории с множеством опций

**Реализация:**
- **Frontend (Pass.cshtml):** `<select>`
- **Backend:** Сохраняется в `UserAnswer.SelectedOptionId`
- **Аналитика:** Столбчатая диаграмма (Bar Chart) или круговая диаграмма

**Код реализации:**
```csharp
case QuestionType.Dropdown:
    <select class="dropdown-select" 
            name="Answers[@question.Id].SelectedOptionIds"
            @(question.IsRequired ? "required" : "")>
        <option value="">Выберите вариант</option>
        @foreach (var option in question.Options)
        {
            <option value="@option.Id">@option.Text</option>
        }
    </select>
    break;
```

**Аналитика:**
- Тип визуализации: **Столбчатая диаграмма (Bar Chart)**
- График: Chart.js - Bar Chart
- Метрики: Количество выборов каждого варианта, процентное соотношение
- Преимущество: Экономит место на странице при большом количестве вариантов

---

### 6. 📁 FileUpload - Загрузка файлов

**Описание:** Возможность загрузить файл (изображение, документ и т.д.).

**Использование:**
- Резюме (PDF, DOC)
- Документы
- Изображения
- Портфолио

**Реализация:**
- **Frontend (Pass.cshtml):** `<input type="file">` с кастомным стилем
- **Backend:** Сохраняется путь к файлу в `UserAnswer.TextAnswer` или отдельная таблица
- **Аналитика:** Список загруженных файлов с возможностью скачать

**Код реализации:**
```csharp
case QuestionType.FileUpload:
    <div class="file-upload-area">
        <input type="file" 
               id="file_@question.Id"
               name="Answers[@question.Id].FileUpload"
               class="file-input"
               @(question.IsRequired ? "required" : "")>
        <label for="file_@question.Id" class="file-label">
            <i class="fas fa-cloud-upload-alt"></i>
            <span>Нажмите для выбора файла</span>
        </label>
    </div>
    break;
```

**Аналитика:**
- Тип визуализации: **Список файлов с превью и ссылками для скачивания**
- График: Не требуется
- Метрики: Количество загруженных файлов, типы файлов, размеры
- Особенность: Требуется обработка загрузки файлов на сервере

---

### 7. 📊 Scale - Шкала (линейная)

**Описание:** Линейная шкала от 1 до 5 для оценки.

**Использование:**
- Удовлетворенность (1-5)
- Вероятность рекомендации
- Оценка качества
- Согласие/несогласие по шкале

**Реализация:**
- **Frontend (Pass.cshtml):** Радио-кнопки с номерами 1-5
- **Backend:** Сохраняется в `UserAnswer.SelectedOptionId` (Order = 1-5)
- **Аналитика:** Линейный график (Line Chart) + средний балл

**Код реализации:**
```csharp
case QuestionType.Scale:
    <div class="scale-wrapper">
        @for (int i = 1; i <= 5; i++)
        {
            <label class="scale-item">
                <span>@i</span>
                <input type="radio"
                       name="Answers[@question.Id].SelectedOptionIds"
                       value="@optionId"
                       @(question.IsRequired ? "required" : "")>
            </label>
        }
    </div>
    <div class="scale-labels">
        <span>Низкая</span>
        <span>Высокая</span>
    </div>
    break;
```

**Аналитика:**
- Тип визуализации: **Линейный график (Line Chart)**
- График: Chart.js - Line Chart с заполнением
- Метрики: 
  - Средний балл (Average Score)
  - Распределение по значениям шкалы
  - Количество ответов на каждое значение
- Визуализация: Показывает тренд распределения оценок

---

### 8. ⭐ Rating - Оценка (звезды)

**Описание:** Рейтинговая система от 1 до 5 звезд.

**Использование:**
- Оценка качества продукта
- Рейтинг сервиса
- Общая удовлетворенность
- Впечатления от опыта

**Реализация:**
- **Frontend (Pass.cshtml):** Радио-кнопки со звездами ⭐
- **Backend:** Сохраняется в `UserAnswer.SelectedOptionId` (Order = 1-5)
- **Аналитика:** Столбчатая диаграмма (Bar Chart) с градиентом + средний рейтинг

**Код реализации:**
```csharp
case QuestionType.Rating:
    <div class="rating-wrapper">
        @for (int i = 1; i <= 5; i++)
        {
            <label class="star-rating-item">
                <input type="radio"
                       name="Answers[@question.Id].SelectedOptionIds"
                       value="@optionId"
                       @(question.IsRequired ? "required" : "")>
                <span class="star-icon">⭐</span>
            </label>
        }
    </div>
    break;
```

**Аналитика:**
- Тип визуализации: **Столбчатая диаграмма с градиентом (Bar Chart)**
- График: Chart.js - Bar Chart с цветовым градиентом
- Метрики:
  - Средний рейтинг (Average Rating)
  - Распределение по звездам
  - Процентное соотношение оценок
- Визуализация: Градиент от светлого к темному зеленому цвету

---

### 9. 🔲 CheckboxGrid - Сетка флажков

**Описание:** Таблица с чекбоксами для множественного выбора по нескольким параметрам.

**Использование:**
- Оценка нескольких параметров одновременно
- Матрица выбора
- Множественные критерии оценки

**Реализация:**
- **Frontend (Pass.cshtml):** Сетка чекбоксов
- **Backend:** Сохраняется несколько записей в `UserAnswer`
- **Аналитика:** Столбчатая диаграмма для каждого варианта

**Код реализации:**
```csharp
case QuestionType.CheckboxGrid:
    <div class="checkbox-grid">
        @foreach (var option in question.Options.OrderBy(o => o.Order))
        {
            <label class="checkbox-option">
                <input type="checkbox"
                       name="Answers[@question.Id].SelectedOptionIds"
                       value="@option.Id">
                <span class="option-text">@option.Text</span>
            </label>
        }
    </div>
    break;
```

**Аналитика:**
- Тип визуализации: **Столбчатая диаграмма (Bar Chart)**
- График: Chart.js - Bar Chart
- Метрики: Количество выборов каждого варианта
- Особенность: Аналогично MultipleChoice, но с возможностью группировки

---

### 10. ⚪ RadioGrid - Сетка (множественный выбор)

**Описание:** Таблица с радио-кнопками для выбора одного варианта из группы.

**Использование:**
- Сравнение вариантов по критериям
- Матрица выбора одного варианта
- Оценка по нескольким параметрам

**Реализация:**
- **Frontend (Pass.cshtml):** Сетка радио-кнопок
- **Backend:** Сохраняется в `UserAnswer.SelectedOptionId`
- **Аналитика:** Круговая диаграмма или столбчатая диаграмма

**Код реализации:**
```csharp
case QuestionType.RadioGrid:
    <div class="radio-grid">
        @foreach (var option in question.Options.OrderBy(o => o.Order))
        {
            <label class="radio-option">
                <input type="radio"
                       name="Answers[@question.Id].SelectedOptionIds"
                       value="@option.Id"
                       @(question.IsRequired ? "required" : "")>
                <span class="option-text">@option.Text</span>
            </label>
        }
    </div>
    break;
```

**Аналитика:**
- Тип визуализации: **Круговая диаграмма (Pie Chart)**
- График: Chart.js - Pie Chart
- Метрики: Распределение выборов по вариантам, процентное соотношение

---

### 11. 🗓️ Date - Дата

**Описание:** Выбор даты через календарь.

**Использование:**
- День рождения
- Дата события
- Планируемая дата
- Срок выполнения

**Реализация:**
- **Frontend (Pass.cshtml):** `<input type="date">`
- **Backend:** Сохраняется в `UserAnswer.TextAnswer` (формат ISO: YYYY-MM-DD)
- **Аналитика:** Список дат или гистограмма по датам

**Код реализации:**
```csharp
case QuestionType.Date:
    <input type="date" class="input-line date-input"
           name="Answers[@question.Id].TextAnswer"
           @(question.IsRequired ? "required" : "")>
    break;
```

**Аналитика:**
- Тип визуализации: **Список дат или временная шкала**
- График: Не требуется (или гистограмма по месяцам/годам)
- Метрики: 
  - Количество ответов
  - Распределение по датам
  - Минимальная/максимальная дата
- Особенность: Можно группировать по месяцам/годам для анализа

---

### 12. 🕐 Time - Время

**Описание:** Выбор времени.

**Использование:**
- Время встречи
- Предпочтения по времени
- Расписание
- Время события

**Реализация:**
- **Frontend (Pass.cshtml):** `<input type="time">`
- **Backend:** Сохраняется в `UserAnswer.TextAnswer` (формат HH:mm)
- **Аналитика:** Список времени или гистограмма по часам

**Код реализации:**
```csharp
case QuestionType.Time:
    <input type="time" class="input-line time-input"
           name="Answers[@question.Id].TextAnswer"
           @(question.IsRequired ? "required" : "")>
    break;
```

**Аналитика:**
- Тип визуализации: **Список времени или гистограмма по часам**
- График: Не требуется (или столбчатая диаграмма по часам)
- Метрики:
  - Количество ответов
  - Распределение по часам дня
  - Наиболее популярное время
- Особенность: Можно группировать по часам для анализа предпочтений

---

## 📈 Типы графиков для аналитики

### Круговая диаграмма (Pie Chart)
**Используется для:**
- SingleChoice
- RadioGrid
- Dropdown (опционально)

**Библиотека:** Chart.js
**Цвета:** Зеленая палитра с градиентами

### Столбчатая диаграмма (Bar Chart)
**Используется для:**
- MultipleChoice
- CheckboxGrid
- Dropdown
- Rating (с градиентом)

**Библиотека:** Chart.js
**Цвета:** Зеленая палитра, для Rating - градиент

### Линейный график (Line Chart)
**Используется для:**
- Scale

**Библиотека:** Chart.js
**Особенности:** С заполнением, показывает тренд распределения

### Список ответов
**Используется для:**
- ShortText
- ParagraphText
- Date
- Time
- FileUpload

**Особенности:** Прокручиваемый список с форматированием

---

## 🔧 Текущий статус реализации

### ✅ Полностью реализовано:
1. ShortText - Текст (строка)
2. ParagraphText - Текст (абзац)
3. SingleChoice - Один из списка
4. MultipleChoice - Несколько из списка
5. Scale - Шкала
6. Rating - Оценка
7. Dropdown - Раскрывающийся список
8. Date - Дата
9. Time - Время
10. FileUpload - Загрузка файлов
11. CheckboxGrid - Сетка флажков
12. RadioGrid - Сетка (множественный выбор)

### ⚠️ Требует доработки в аналитике:
- **Dropdown** - нужна обработка в контроллере Results
- **CheckboxGrid** - нужна обработка в контроллере Results
- **RadioGrid** - нужна обработка в контроллере Results
- **Date** - нужна обработка в контроллере Results и визуализация
- **Time** - нужна обработка в контроллере Results и визуализация
- **FileUpload** - нужна обработка в контроллере Results и отображение файлов

---

## 📝 Запросы на реализацию

### 1. Обработка Dropdown в аналитике
**Файл:** `Controllers/SurveyController.cs` (метод `Results`)
**Действие:** Добавить обработку `QuestionType.Dropdown` аналогично `SingleChoice`

### 2. Обработка CheckboxGrid в аналитике
**Файл:** `Controllers/SurveyController.cs` (метод `Results`)
**Действие:** Добавить обработку `QuestionType.CheckboxGrid` аналогично `MultipleChoice`

### 3. Обработка RadioGrid в аналитике
**Файл:** `Controllers/SurveyController.cs` (метод `Results`)
**Действие:** Добавить обработку `QuestionType.RadioGrid` аналогично `SingleChoice`

### 4. Обработка Date в аналитике
**Файл:** `Controllers/SurveyController.cs` (метод `Results`)
**Действие:** Добавить обработку `QuestionType.Date` с группировкой по датам

### 5. Обработка Time в аналитике
**Файл:** `Controllers/SurveyController.cs` (метод `Results`)
**Действие:** Добавить обработку `QuestionType.Time` с группировкой по часам

### 6. Обработка FileUpload в аналитике
**Файл:** `Controllers/SurveyController.cs` (метод `Results`)
**Действие:** Добавить обработку `QuestionType.FileUpload` с отображением списка файлов

### 7. Улучшение визуализации в Results.cshtml
**Файл:** `Views/Survey/Results.cshtml`
**Действие:** Добавить отображение для всех типов вопросов с соответствующими графиками

### 8. Улучшение analytics.js
**Файл:** `wwwroot/js/analytics.js`
**Действие:** 
- Улучшить качество графиков (антиалиасинг)
- Добавить поддержку всех типов вопросов
- Улучшить цветовую палитру
- Добавить анимации

---

## 🎨 Визуальные улучшения

### Проблемы, требующие исправления:
1. **Размытость графиков** - необходимо улучшить рендеринг Chart.js
2. **Цветовая палитра** - расширить палитру для большего количества вариантов
3. **Адаптивность** - улучшить отображение на мобильных устройствах
4. **Анимации** - добавить плавные переходы при загрузке графиков
5. **Легенды** - улучшить читаемость легенд на графиках

### Решения:
- Использовать `devicePixelRatio` для четкости графиков
- Расширить цветовую палитру
- Добавить адаптивные размеры для мобильных устройств
- Улучшить стили для легенд и подписей

---

## 📚 Связанные файлы

### Модели:
- `Models/Question.cs` - определение типов вопросов
- `Models/AnswerOption.cs` - варианты ответов
- `Models/Answer.cs` - ответы пользователей
- `Models/UserAnswer.cs` - связь ответов с вопросами

### Представления:
- `Views/Public/Pass.cshtml` - страница прохождения опроса
- `Views/Survey/Constructor.cshtml` - конструктор опросов
- `Views/Survey/Results.cshtml` - страница результатов
- `Views/Survey/Preview.cshtml` - предпросмотр опроса

### Контроллеры:
- `Controllers/SurveyController.cs` - обработка опросов и результатов
- `Controllers/PublicController.cs` - публичный доступ к опросам

### JavaScript:
- `wwwroot/js/analytics.js` - графики и аналитика
- `wwwroot/js/constructor.js` - логика конструктора

### Стили:
- `wwwroot/css/constructor.css` - стили конструктора
- `wwwroot/css/surveys.css` - стили опросов

---

## 🚀 План дальнейшего развития

1. **Улучшение аналитики:**
   - Добавить экспорт графиков в PNG/PDF
   - Добавить фильтры по датам
   - Добавить сравнение результатов

2. **Новые типы вопросов:**
   - Матрица оценок (Rating Matrix)
   - Ранжирование (Ranking)
   - Слайдер (Slider)
   - Время начала/окончания (Time Range)

3. **Улучшение визуализации:**
   - Интерактивные графики
   - 3D визуализация
   - Интеграция с внешними сервисами аналитики

---

*Документ обновлен: 2024*

