# Руководство по ручному применению миграции

## 🎯 Задача
Добавить поля профиля пользователя в базу данных:
- `FirstName` - Имя
- `LastName` - Фамилия
- `ProfileImagePath` - Путь к фото профиля
- `ShowPhoneToPublic` - Видимость телефона

## 📋 Методы применения миграции

### Метод 1: Через Entity Framework (Рекомендуется)

#### Вариант A: Через Package Manager Console в Visual Studio

1. Откройте **Visual Studio**
2. Выберите меню **Tools → NuGet Package Manager → Package Manager Console**
3. Выполните команду:

```powershell
Update-Database
```

#### Вариант B: Через командную строку (cmd.exe, НЕ PowerShell)

1. Откройте **Командную строку** (cmd.exe)
2. Перейдите в папку проекта:

```cmd
cd /d "D:\4 курс\Курсоавая работа 4 курс 1 семестр\CurseProject\NovoyaHope\NovoyaHope"
```

3. Выполните команду:

```cmd
dotnet ef database update
```

#### Вариант C: Через Terminal в Visual Studio Code

1. Откройте **Terminal** в VS Code
2. Убедитесь, что вы в папке проекта
3. Выполните:

```bash
dotnet ef database update
```

---

### Метод 2: Через SQL скрипт (Если dotnet ef не работает)

1. Откройте **SQL Server Management Studio (SSMS)**
2. Подключитесь к вашей базе данных
3. Откройте файл `add-profile-fields.sql` из папки проекта
4. **ВАЖНО:** Измените первую строку на имя вашей базы данных:
   ```sql
   USE [NovoyaHopeDB];  -- Измените это на имя вашей БД
   ```
5. Нажмите **F5** или кнопку **Execute**

---

### Метод 3: Создать миграцию заново

Если текущая миграция повреждена:

#### Шаг 1: Удалить существующую миграцию

В командной строке (cmd.exe):

```cmd
cd /d "D:\4 курс\Курсоавая работа 4 курс 1 семестр\CurseProject\NovoyaHope\NovoyaHope"
dotnet ef migrations remove
```

#### Шаг 2: Создать новую миграцию

```cmd
dotnet ef migrations add AddUserProfileFields
```

#### Шаг 3: Применить миграцию

```cmd
dotnet ef database update
```

---

## 🔍 Проверка успешности миграции

### Проверка через SQL

Выполните в SSMS:

```sql
-- Проверка существования столбцов
SELECT 
    c.name AS ColumnName,
    t.name AS DataType,
    c.max_length AS MaxLength,
    c.is_nullable AS IsNullable
FROM sys.columns c
INNER JOIN sys.types t ON c.user_type_id = t.user_type_id
WHERE c.object_id = OBJECT_ID('[Identity].[User]')
    AND c.name IN ('FirstName', 'LastName', 'ProfileImagePath', 'ShowPhoneToPublic')
ORDER BY c.name;

-- Проверка истории миграций
SELECT * FROM [Identity].[__EFMigrationsHistory]
WHERE MigrationId LIKE '%AddUserProfileFields%';
```

Должны появиться 4 строки с новыми столбцами.

### Проверка через приложение

1. Запустите проект:
   ```cmd
   dotnet run
   ```

2. Откройте браузер: `https://localhost:5001` (или другой порт)

3. Войдите в систему или зарегистрируйтесь

4. Кликните на аватар в правом верхнем углу

5. Выберите "Мой профиль"

6. Если страница открылась без ошибок - миграция успешна! ✅

---

## ❌ Устранение проблем

### Проблема: "No project was found"

**Решение:**
```cmd
cd /d "D:\4 курс\Курсоавая работа 4 курс 1 семестр\CurseProject\NovoyaHope\NovoyaHope"
dir *.csproj
```
Убедитесь, что вы в правильной папке.

### Проблема: "Could not connect to the database"

**Решение:**
1. Проверьте строку подключения в `appsettings.json`
2. Убедитесь, что SQL Server запущен
3. Проверьте доступность базы данных:
   ```sql
   SELECT @@SERVERNAME, DB_NAME();
   ```

### Проблема: "Column names in each table must be unique"

**Решение:** Столбцы уже существуют. Проверьте:
```sql
SELECT * FROM [Identity].[__EFMigrationsHistory];
```

Если миграция `AddUserProfileFields` уже там есть, значит она применена.

### Проблема: "Build failed"

**Решение:**
```cmd
dotnet clean
dotnet build
```

---

## 📝 Альтернативный способ: Полное пересоздание БД

⚠️ **ВНИМАНИЕ:** Это удалит все данные!

```cmd
# Удалить базу данных
dotnet ef database drop

# Создать заново со всеми миграциями
dotnet ef database update
```

---

## ✅ Проверочный список

После применения миграции проверьте:

- [ ] Команда `dotnet ef database update` выполнена без ошибок
- [ ] В таблице `[Identity].[User]` есть 4 новых столбца
- [ ] В `__EFMigrationsHistory` есть запись о миграции
- [ ] Проект компилируется: `dotnet build`
- [ ] Приложение запускается: `dotnet run`
- [ ] Страница профиля открывается без ошибок
- [ ] Можно заполнить поля профиля

---

## 🎉 Что дальше?

После успешного применения миграции:

1. **Запустите приложение:**
   ```cmd
   dotnet run
   ```

2. **Войдите в систему** или зарегистрируйтесь

3. **Откройте профиль:**
   - Кликните на аватар в навигации
   - Выберите "Мой профиль"

4. **Заполните данные:**
   - Добавьте имя и фамилию
   - Загрузите фото профиля
   - Укажите телефон
   - Настройте приватность

5. **Протестируйте функции:**
   - Выпадающее меню
   - Модальное окно (клик на email)
   - Изменение пароля
   - Копирование данных

---

## 📞 Дополнительная помощь

Если ничего не помогло:

1. Проверьте версию .NET SDK:
   ```cmd
   dotnet --version
   ```
   Должна быть 8.0 или выше

2. Проверьте Entity Framework Core Tools:
   ```cmd
   dotnet ef --version
   ```

3. Установите/обновите EF Core Tools:
   ```cmd
   dotnet tool install --global dotnet-ef
   dotnet tool update --global dotnet-ef
   ```

---

**Дата:** 27 ноября 2025  
**Версия:** 1.0

